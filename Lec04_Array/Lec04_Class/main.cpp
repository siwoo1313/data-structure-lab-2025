#include "Complex.h"#include "Car.h"int main(){	//Complex a, b, c;	//a.read("A= ");	//a.print("A = ");	Car myCar;	Car momsCar(10, "K5", 2);	SportsCar mySecondCar;	myCar.whereAmI();	momsCar.whereAmI();	myCar.changeGear(3);	myCar.display();	mySecondCar.setTurbo(true);}#include "Music.h"int main() {	//music streaming service¸¦ »ý¼º	MusicStreamingService my_service("spotify");	my_service.addMusic("souvlaki space station", "slowdive", "souvlaki", "1993");	my_service.addMusic("only shallow", "my bloody valentine", "loveless", "1991");	my_service.addMusic("pop out", "playboi carti", "i am music", "2024");	my_service.addMusic("magnolia", "playboi carti", "playboi carti", "2017");	//search music by title	string music_title;	cout << "Enter the Music Title :";	cin >> music_title;	Music* result = my_service.searchByTitle(music_title);	if (result != NULL) {		cout << "Found": << result->getTitle() << "by" << result->getArtist() << endl;	}	else {		cout << "notfound" << endl;	}	//search music by title	string artist_name;	cout << "Enter the Artist Name: ";	cin >> artist_name;	vector<Music*> artist_result = my_service.searchByArtist(artist_name);	if (artist_result.size() > 0) {		cout << "Found" << artist_result.size() << " songs by " << artist_name << " : " << endl;		for (int i = 0; i < artist_result.size(); i++) {			cout << artist_result[i]->getTitle() << endl;		}	}	else {		cout << "Not found" << endl;	}	return 0;}